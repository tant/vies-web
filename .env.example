# Database
DATABASE_URL=postgresql://vies:your_password@localhost:5432/vies

# PayloadCMS — generate a strong random secret (64+ chars) for production
PAYLOAD_SECRET=YOUR_SECRET_HERE

# Public site URL — used for SEO, live preview, media URLs
NEXT_PUBLIC_SITE_URL=http://localhost:3000
